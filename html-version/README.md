# HTML 版

这是不依赖 React、Vite 或打包工具的纯静态版本。

文件结构：

- `index.html`
- `styles.css`
- `script.js`

使用方式：

1. 直接双击 `index.html`。
2. 或放到任意静态网站目录中。
3. 如果你有本地服务器，也可以在该目录下启动后访问。

这个版本保留了原应用的核心功能：

- 典型序列列表切换
- 自定义序列表达式输入
- 数值 DTFT 幅度谱与相位谱
- `n` 与 `ω` 范围控制
- 两位小数显示
- 文件上传接口预留
